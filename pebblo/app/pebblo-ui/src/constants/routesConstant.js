const PROXY = `http://127.0.0.1:${window.location.port}`;
const PREFIX = "/pebblo";

export const DASHBOARD_ROUTE = PREFIX + "/";
export const APP_DETAILS_ROUTE = PREFIX + "/appDetails";
export const GET_REPORT = `${PROXY}${PREFIX}/getReport`;
